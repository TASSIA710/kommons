name: Build
on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: gradle
    strategy:
      fail-fast: false
      matrix:
        module:
          - project: ":commons-bom"
            name: "Bill of Materials"
          - project: ":commons-encoding"
            name: "Encoding"
          - project: ":commons-ini"
            name: "INI Files"
          - project: ":commons-logging"
            name: "Logging"
          - project: ":commons-parser"
            name: "Parser"
          - project: ":commons-security"
            name: "Security"
    name: ${{ matrix.module.name }}
    steps:
      - name: Checkout Repository
        uses: "actions/checkout@v2"
      - name: Start Gradle Daemon
        run: "gradle init"
      - name: Build - ${{ matrix.module.name }}
        run: "gradle ${{ matrix.module.project }}:assemble"
      - name: Test - ${{ matrix.module.name }}
        run: "gradle ${{ matrix.module.project }}:test"

