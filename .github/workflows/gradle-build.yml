name: Build
on: [push, workflow_dispatch]

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    container:
      image: gradle
    steps:
      - name: Start Gradle Daemon
        run: "gradle init"
      - name: Build
        run: "gradle build"
      - name: Test
        run: "gradle test"

